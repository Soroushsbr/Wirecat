<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_card_stroke"
    android:padding="14dp"
    android:layout_marginBottom="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/textSessionType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:background="@drawable/bg_pill_muted"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:textStyle="bold"
                android:textSize="10sp"
                android:textColor="@color/wc_accent"
                android:text="MONITOR" />

            <TextView
                android:id="@+id/textSessionDate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="?attr/wcColorTextPrimary"
                android:textStyle="bold"
                android:textSize="14sp"
                android:text="Aug 15, 2026 - 10:00" />

        </LinearLayout>

        <TextView
            android:id="@+id/textSessionSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="?attr/wcColorTextSecondary"
            android:textSize="12sp"
            android:text="Total: 0 B \u2022 Duration: 00:00:00" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/btnDeleteSession"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginStart="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_delete"
        app:tint="?attr/wcColorTextSecondary"
        android:contentDescription="@string/history_delete_one" />

</LinearLayout>
